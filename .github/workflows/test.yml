name: test-project
on:
  push: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Get code
      uses: actions/checkout@v3
    - name: Install NodeJs
      uses: actions/setup-node@v3
    - name: Install dependencies
      run: npm ci
    - name: run tests
      run: npm test